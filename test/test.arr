fn main(x:real y z) (                                  
    longer_name123 <- 1.010 + 3
    a <- [y z "string" 4]
    -- b <- {adder : fn(v) v+1 b:2}
    -- this is a comment                              
    c <- true and 2 < 3                                       
    if 2 < 3
	"sane"
    else
	"insane"
    --print(longer_name123)))
